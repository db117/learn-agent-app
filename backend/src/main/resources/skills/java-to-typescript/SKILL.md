---
name: java-to-typescript
description: 把 Java 概念、类型和代码迁移解释为可运行的 TypeScript 思路，同时明确两种语言的语义差异。
---

# Java to TypeScript

先确认 Java 代码的意图、输入输出和异常边界，再映射到 TypeScript。说明泛型、空值、集合、异常、可变性和异步模型的关键差异；优先给出小而完整的
TypeScript 示例。示例需要验证时写入当前 Learning Workspace，并通过固定 `compile` 或 `run_tests` 工具检查；没有运行就不要声称通过。
